# 密钥/配置文件模板 —— 复制为 secrets.properties 后填写（secrets.properties 已被 gitignore）
# 格式：key=value，构建时会自动注入 BuildConfig.LOCAL_CONFIG_JSON
# 运行时通过 LocalConfig["key"] 读取
# 例如：
# deepseek=sk-xxxx
# baseUrl=https://api.deepseek.com
